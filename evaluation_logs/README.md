This directory stores artifacts produced by smoke tests and evaluation runs.

Keep small diagnostic files here (e.g., `run_agent_result.json`). Large model files should remain managed by the user (Ollama).